1. Javascript 전역함수
    (1) eval()
        eval("2*3+4*6") => 30
    
    (2) parseInt()
        문자열을 정수로 변환
        ParseInt()
    
    (3) isNaN()
        숫자가 아니면 true 숫자면 false

    (4) isFinite()
        숫자면 true 숫자가 아니면 false
    
2. DOM(Document Object Model)
    HTML 태그들을 하나씩 오브젝트화한 것
    HTML 페이지의 내용과 모양을 제어하기 위해 사용되는 객체
    HTML 태그 당 DOM 객체가 하나씩 생성
    HTML 태그의 포함 관계에 따라 부모-자식으로 결정